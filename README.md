# Capacity Forecasting – 7 Day Build

Goal: build a minimal pipeline that uses service metrics
(QPS / CPU / latency) to do forecasting, anomaly detection,
and generate automated reports.

This repo records daily progress and decisions.
